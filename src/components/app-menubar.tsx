import { Search } from "./ui/search"

export const AppMenubar = () => {
    return (
        <header className=" w-full p-3 flex justify-between">
            <Search />
            <p>icon</p>
        </header>
    )
}